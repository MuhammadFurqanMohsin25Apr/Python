# KNOWN
# Radius (r) in meters:
r = float(input("Enter the radius:"))

# Linear velocity (v) in meters/second:
v = float(input("Enter the linear velocity:"))

# We need to find angular velocity:

# FORMULA:
ω = v / r

print("The magnitude of the angular velocity is:", ω, "radians/second")
