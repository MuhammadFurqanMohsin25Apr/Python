# KNOWN
# Radius (r) in centimeters:
r = float(input("Enter the radius:"))

# Angular Speed (ω) in radians/second:
ω = float(input("Enter the Angular Speed:"))

# We need to find linear velocity:

# FORMULA:
v = r * ω

print("The magnitude of the linear velocity is:", v, "centimeters/second")
